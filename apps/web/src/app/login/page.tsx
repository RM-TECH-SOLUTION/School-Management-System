'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { GraduationCap, ShieldCheck } from 'lucide-react';
import { api } from '@/lib/api'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { Card } from '@/components/ui/card';
export default function Login(){const router=useRouter(),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);try{const data=await api('/auth/login',{method:'POST',body:JSON.stringify({email:f.get('email'),password:f.get('password')})});localStorage.setItem('school_token',data.accessToken);localStorage.setItem('school_user',JSON.stringify(data.user));router.push('/dashboard')}catch(e:any){setError(e.message||'Unable to sign in')}finally{setLoading(false)}}return <main className="relative grid min-h-screen place-items-center overflow-hidden bg-ink p-5"><div className="absolute -left-24 top-16 size-96 rounded-full bg-gold/20 blur-3xl"/><div className="absolute -bottom-32 right-10 size-[32rem] rounded-full border border-paper/10"/><div className="relative grid w-full max-w-5xl overflow-hidden rounded-3xl border border-white/10 bg-white/5 shadow-2xl backdrop-blur md:grid-cols-[1.1fr_.9fr]"><section className="hidden min-h-[620px] flex-col justify-between bg-[radial-gradient(circle_at_20%_20%,#527562,transparent_35%),#17221c] p-10 text-paper md:flex"><div className="flex items-center gap-3"><GraduationCap className="text-gold"/><span className="display text-2xl">northstar.</span></div><div><p className="eyebrow text-gold">Operations platform</p><h1 className="display mt-5 text-6xl leading-[.9]">The work behind every bright future.</h1></div><div className="flex items-center gap-2 text-xs text-paper/55"><ShieldCheck className="size-4 text-gold"/> Secure school access</div></section><section className="bg-paper p-7 sm:p-12"><p className="eyebrow text-moss">Secure access</p><h2 className="display mt-4 text-5xl">Welcome back.</h2><p className="mt-3 text-sm text-ink/60">Sign in to Northstar school operations.</p><Card className="mt-8 p-5"><form onSubmit={submit} className="space-y-5"><label className="block text-sm font-medium">Email<Input name="email" type="email" defaultValue="admin@northstar.edu" required className="mt-2"/></label><label className="block text-sm font-medium">Password<Input name="password" type="password" defaultValue="Admin@12345" required className="mt-2"/></label>{error&&<p className="rounded-md bg-red-50 p-3 text-sm text-red-700">{error}</p>}<Button disabled={loading} className="w-full">{loading?'Signing in…':'Sign in'}</Button></form></Card><p className="mt-5 text-xs text-ink/45">Local development credentials are pre-filled. The app uses a local fallback only when the API is offline.</p></section></div></main>}
