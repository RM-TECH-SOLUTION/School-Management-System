import type { Metadata } from 'next'; import './globals.css'; export const metadata:Metadata={title:{default:'Northstar International School',template:'%s | Northstar'},description:'A generous education for curious minds, courageous hearts and a changing world.'}; export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
